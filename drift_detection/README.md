1 . if we face drift and we run terraform apply then we revert back to desired configuration . Desired configuration means which we have writen inside main.tf file

2. we can change terraform file and run apply command if we want the manual changes in our main.tf

